extends ../layout

block content
  .container
      form(action='/mot_de_passe_oublie', method='POST', class="d-block w-50")
        div.reset_password
          label Entre ton nouveau mot de passe
          input(class="form-control", type="email", placeholder="email", name="email")
        div.confirme_password
          label Confirme ton mot de passe
          input(class="form-control", type="email", placeholder="email", name="email")
        button(id="btn-formulaireAndConnexion", class="btn btn-primary", type="submit") Enregistrer