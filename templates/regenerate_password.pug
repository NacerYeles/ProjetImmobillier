extends layout

block content
  .container
      form(action='/mot_de_passe_oublie', method='POST', class="d-block w-50")
        div.label
          h2 Entre ton adresse mail
        div.username
          input(class="form-control", type="email", placeholder="email", name="email")
        button(id="btn-formulaireAndConnexion", class="btn btn-primary", type="submit") Connexion